<% Critere.tagged_with(reading.tags[0]).each_with_index do |tag, index| %>
  <div id="<%= index %>">
    <div ><%= tag.name %></div>
    <%= render 'books/slider_passif', pos: 3, status: "slide#{index}"  %>
  </div>
<% end %>
