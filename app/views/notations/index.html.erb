

<div class="container" style="margin-top: 100px">
  <h1>notation</h1>
  <div class="notations mb-5">
    <% @taglist.each_with_index do |tag, index| %>
    <div id="<%= index %>">
      <div ><%= tag.name %></div>
      <%= render 'slider', status: "slide#{tag.id}"  %>
    </div>
    <% end %>
    <hr>
    <div id="<%= 99 %>" class="mt-3">
      <div >Ce livre vous a plu ?</div>
      <%= render 'slider', status: "slide99"  %>
    </div>
  </div>


  <div>

<p>Voulez-vous écrire un commentaire ?</p>

    <%= link_to "Non merci", post_notation_path(@reading, status: 'false'), method: 'post',  class: "btn btn-lg btn-danger d-block w-100 mb-5"%>

    <%= link_to "ok", post_notation_path(@reading, status: 'true'), method: 'post',  class: "btn btn-lg btn-primary d-block w-100 mb-5"%>

  </div>







